{
  "name": "pokemon-monorepo",
  "version": "0.0.1",
  "author": "Jadir Hervias Salas",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "re-build": "docker compose build --no-cache",
    "start": "docker compose up -d",
    "start:watch": "docker compose up",
    "stop": "docker compose down"
  }
}
